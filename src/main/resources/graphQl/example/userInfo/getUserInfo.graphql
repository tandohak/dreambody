# 회원정보 조회
query GetUserInfo {
    userInfo {
        id
        currentWeight
        dailyIntakeCalorie
        dateOfBirth
        goalWeight
        height
        genderType
        goalType
        activityType
        user {
            id
            name
            email
            emailVerified
            provider
            providerId
        }
    }
}