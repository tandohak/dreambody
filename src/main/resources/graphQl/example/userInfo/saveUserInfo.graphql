# 회원 정보 저장
mutation saveUserInfo($userInfoRequest: UserInfoRequest) {
    saveUserInfo(userInfoRequest: $userInfoRequest) {
        currentWeight
        goalWeight
        height
        dateOfBirth
        genderType
        goalType
        activityType
    }
}