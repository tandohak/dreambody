mutation saveUserInfo($requestUserInfo: RequestUserInfo) {
    saveUserInfo(requestUserInfo: $requestUserInfo) {
        currentWeight
        goalWeight
        height
        dateOfBirth
        gender {
            id
            gender
        }
        goal {
            id
            goal
        }
        activity {
            id
            activity
        }
    }
}